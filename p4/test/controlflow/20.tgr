let
    var pescado : int;
    var carne : int;
    var a : int;
    var b : int; 
    var res1 : int;
    var res2 : int;
    var res3 : int;
    var c : int;
in
    a := 100;
    b := a;
    if c > 100 then
        res1 := carne + a;
        for a := 1 to 100 do
            carne := carne + a;
        enddo;
    else
        res2 := carne + a;
    endif;
    res3 := carne + a;/*CSE*/
end
