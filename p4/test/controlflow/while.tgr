let
    var pescado : int;
    var carne : int;
    var a : int;
    var b : int;
    var res : int;
    var c : int;
    func twicef ( n : int ) : int begin
        n := 2 + 5;
        n := 5;
        if n > 10 then
            return n + n;
        endif;
        n := n + 1;
        n := n + 5;
        return 3;
    end;
in
    b := a;

    c := twicef(b);

    if carne = 100 then
        a := a + 1;
        for b := 1 to 100 do
            a := a / 2;
            while res <> 0 do
                res := a + b;
                break;
            enddo;
            a := a * 2;
        enddo;
        b := carne + a;
    endif;
end
