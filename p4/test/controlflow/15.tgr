let
    var pescado : int;
    var carne : int;
    var a : int;
    var b : int; 
    var res1 : int;
    var res2 : int;
    var res3 : int;
    var res4 : int;
    var res5 : int;
in
    for b := 1 to 100 do
        res1 := a+carne;
    enddo;
    if a < 100 then
        res3 := a+b;
    else
        res4 := a;
    endif;
    res5 := a+carne;/*CSE*/
end
