let
    var pescado : int;
    var carne : int;
    var a : int;
    var b : int; 
    var res1 : int;
    var res2 : int;
    var c : int;
in
    a := 100;
    b := a;
    for a := 1 to 100 do
        carne := a+b;
    enddo;
    c := carne + a;
    if carne = 100 then
        res1 := carne + a;/*CSE*/
    else
        res2 := 0;
    endif;
end
